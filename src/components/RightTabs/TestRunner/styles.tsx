import { Theme } from '@material-ui/core/styles';

const styles = (theme: Theme) => ({
    testTab: {
        backgroundColor: '#fff',
    },
    section: {
        margin: '15px 0'
    }
});

export default styles;
